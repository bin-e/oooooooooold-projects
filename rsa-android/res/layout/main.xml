<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/img_bg"
    android:gravity="center|center"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="2dp"
        android:layout_weight="1"
        android:gravity="center|bottom"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/button_main_mouse"
            android:layout_width="150dp"
            android:layout_height="200dp"
            android:layout_marginRight="2dp"
            android:background="@drawable/ic_button_bg"
            android:drawableTop="@drawable/ic_button_mouse"
            android:padding="40dp"
            android:text="Mouse"
            android:textColor="#FFFFFF"
            android:textSize="20dp" />

        <Button
            android:id="@+id/button_main_help"
            android:layout_width="150dp"
            android:layout_height="200dp"
            android:layout_marginLeft="2dp"
            android:background="@drawable/ic_button_bg"
            android:drawableTop="@drawable/ic_button_wake_on_lan"
            android:padding="40dp"
            android:text="Help"
            android:textColor="#FFFFFF"
            android:textSize="20dp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="2dp"
        android:layout_weight="1"
        android:gravity="center_horizontal"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/button_main_program"
            android:layout_width="150dp"
            android:layout_height="200dp"
            android:layout_marginRight="2dp"
            android:background="@drawable/ic_button_bg"
            android:drawableTop="@drawable/ic_button_program"
            android:padding="40dp"
            android:text="Program"
            android:textColor="#FFFFFF"
            android:textSize="17dp" />

        <Button
            android:id="@+id/button_main_exit"
            android:layout_width="150dp"
            android:layout_height="200dp"
            android:layout_marginLeft="2dp"
            android:background="@drawable/ic_button_bg"
            android:drawableTop="@drawable/ic_button_exit"
            android:padding="40dp"
            android:text="Exit"
            android:textColor="#FFFFFF"
            android:textSize="20dp" />
    </LinearLayout>

</LinearLayout>